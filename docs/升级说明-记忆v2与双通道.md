# 升级说明（历史批次存档 + 最新批次）

## 2026-09-10/11 批次（世界引擎全接入 + 人格活体化）

### 世界引擎 = 独立模型底座
- API 必须独立配置（「她」页底部），不与对话接口共用、不回落；未配置=随机兜底；失败30分钟退避。
- 每晚产出：日记/明日作息/心情/痴迷/流水/念头/秘密/天气/画像/长线/纪念日/社交圈维护；每满7天性格周结算。
- 虚拟社交圈：稳定编制≤6人，按名字合并，旧字符串格式自动兼容；聊天里自然提及。
- 天气：Open-Meteo 真实天气（免费无key，城市=人设城市，坐标缓存），可关（全由她编）。

### 人格活体化
- 六维每日弹性（有因果漂移，±15硬边界，不动八维）；周结算演化（合计≤±5，八维推导基准钳制±25）。
- 称呼真·自动演变（阶段 callHint 进提示词；callOwner=恋人阶段专属昵称；callLock 锁定）。
- 生活节奏区保存修复（原控件空回调永不写值）；百分比滑条全部 0-100%；作息波动上限±600；周末推迟30%~100%随机。

### 遗漏接线（大排查修复）
- 念头/秘密/社交圈/长线/纪念日/天气 → 注入聊天提示词
- 画像注入断链修复（reply 未传 portrait）；提示词六维改读 traits+漂移（原读废弃 personality 字段）
- 世界引擎API卡从「大脑」迁至「她」页；女娲草稿显示移除大五/九型残留

## 2026-07 批次（存档）
- 记忆系统 v2：向量与嵌入模型都在本机，只有"整理成记忆"用云端便宜模型；同时写一份本地镜像兜底（引擎挂了也不丢）；旧记忆迁移可重复执行不会重复写
- 设置大本营 + 双通道（ClawBot 主线 / wxauto 预留）+ 功能状态页
- 详见记忆系统设计.md 与 架构设计.md

## 2026-09-11 黑盒清零批次（变形真接线 + 双向记忆联动）
- 变形状态机：修字段名不匹配（deformLine）与"恢复在生成后才消费"的时序错误；三档风格指令+道歉/自嘲+整合度成长提示真正注入提示词；后台新增变形卡（状态/账本/开关/灵敏度/阈值）。
- 双向记忆：检索补 `who=self` 一路（mem0+本地），提示词分两段注入"她自己的话"与"别人的事"；后台新增双向记忆卡（开关+列表）。
- 六维每日弹性在实况页可见（📊 徽章）；`_behavior()` 丢 `memory` 组导致的静默失效修复；params.historyRounds/maxTokens 生效；typoRate 做成真功能（手滑更正，可调）；toOwner 进提示词。
- 新增 test/audit-config.mjs（配置键消费点审计，禁黑盒可回归）+ test/deform-memory-smoke.mjs（23 项）。
- 工程坑：工作区改名 → pnpm 软链失效（Cannot find package 'silk-wasm'）；已实体化 vendored 四件套。

## 2026-09-11 DSH 0.1.5-rc.1 适配
- **插件市场警告消除**：dsh-llm/dsh-tools 等 6 个 @deepseek-ai/dsh-* + schemastery 从 dependencies 全部迁到 peerDependencies（共享宿主包由宿主提供，插件不再声明owns以免遮蔽宿主版本）；dependencies 只留 4 个真第三方 vendored 包（qrcode/pngjs/dijkstrajs/silk-wasm）。
- peer 范围 `>=0.1.2-rc.1 <0.2.0`（0.1.2 与 0.1.5 双兼容实测），client-ui-slots/react 标 optional。
- **API 兼容验证**：createUserMessage/defineTool/installModelSelection 签名 0.1.5 与 0.1.2 一致；settings.register/applies 保留；0.1.5 彻底删除的 installSettingsSection/settingsNamespace 本插件早已不用。
- **本地实测**：workspace node_modules 全量升级到真 0.1.5-rc.1（+cordis 4.0.2/schemastery 3.18.2/zod 4.4.3 及闭包 14 包，备份在 /tmp/dsh-ai-backup-0.1.0），7 套烟测全绿（boot 26/world 30/life/backup/moments/workshop/smoke）。
- 顺手修活旧测试：smoke.mjs/workshop-smoke.mjs 还引用存档制改造前的 rollbackPersona/_retrieve（同步API）——已改为 archivePersona/switchArchive 与 await _retrieveHybrid。
- 注意：宿主 DSH Desktop 升到带 0.1.5-rc.1 内核的版本后本插件直接可用；client inject 三模块名（dsh-client-runtime/ui-settings/locale）重启后需目检设置面板是否正常加载。

## 2026-09-11 通宵核查批次（用户睡眠期间自动执行）
- 修复7处页签重构后的旧ID跳转（选主人/黑名单/朋友圈操作/导入配置后弹回首页）
- 备份保留天数设置生效化 + 备份内容扩全（世界剧本/演化账本/女娲会话/朋友圈/配置/mem0向量库整目录）
- 嵌入向量设置链路修复（本地模型读取错字段、云端BaseURL被白名单丢弃、MemoryEmbed初始化改读embed组）
- 女娲草稿显示残留清理（大五/九型→MBTI-only）；emojiRate钳制；天气开关异步同步
- 全量回归：world 30项 / boot 26项 / backup / life / moments 全绿
